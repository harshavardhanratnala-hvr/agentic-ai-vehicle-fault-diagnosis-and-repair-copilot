# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# Env / secrets
.env
.env.*
!.env.example

# Data (large/raw files — keep out of git, document sources in docs/)
data/*
!data/.gitkeep

# Media (large files)
media/*.mp4
media/*.mov

# OS
.DS_Store

# Office lock/temp files
~$*

# Node (if frontend added later)
node_modules/
